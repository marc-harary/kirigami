{
    "data": {
        "training_list": "/path/to/training/list",
        "validation_list": "/path/to/validation/list",
        "checkpoint": "checkpoint.pt",
        "best": "best.pt"
    },

    "training": {
        "epochs": 1000,
        "batch_size": 1,
        "shuffle": true,
        "print_frequency": 10
    },

    "loss_func": "torch.nn.BSELoss()",

    "optim": "torch.optim.Adam(model.parameters(), lr=1e-2)",

    "model": [
        "kirigami.nn.SPOT.BlockA(p=0.4)",
        "torch.nn.Conv2d(in_channels=8, out_channels=1, kernel_size=1)",
        "torch.nn.Sigmoid()"
    ]
}
